# Ruleset pf que carga scriptorium-root-setup.sh en /etc/pf-scriptorium.conf
# y aplica el LaunchDaemon com.fran.scriptorium-pf en cada arranque con
# `pfctl -ef`.
#
# Reproduce las anclas por defecto de macOS (las de /etc/pf.conf) para no perder
# las reglas del sistema al sustituir el ruleset principal, y añade el ancla
# `scriptorium` con la redirección 80 -> 8080.
#
# Orden obligado por pf: normalización (scrub) -> traducción (nat, rdr) ->
# filtrado (anchor).
scrub-anchor "com.apple/*"
nat-anchor "com.apple/*"
rdr-anchor "com.apple/*"
dummynet-anchor "com.apple/*"
rdr-anchor "scriptorium"
anchor "com.apple/*"
load anchor "com.apple" from "/etc/pf.anchors/com.apple"
load anchor "scriptorium" from "/etc/pf.anchors/scriptorium"
